# Copy to terraform.tfvars and adjust if needed.
# terraform.tfvars is gitignored (it may hold environment-specific or sensitive values).
# For Phase A the defaults in variables.tf are fine, so this is optional.
region      = "ap-northeast-2"
name_prefix = "sre-roadmap"
